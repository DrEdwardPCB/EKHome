import { createSignal } from "solid-js";
function InterestRateCalculator() {
	const [Principal, SetPrincipal] = createSignal("");
	const [AnnualPercentale, SetAnnualPercentale] = createSignal();
	return <div></div>;
}
